# Практическая работа: REST API + Docker

Пишем простой REST API на FastAPI и запускаем его в Docker-контейнере.

---

## Структура проекта

```
classroom5/
├── main.py          # API - здесь весь твой код
├── Dockerfile       # описание образа
└── requirements.txt # зависимости Python
```

---

## REST API

### Задача

Реализовать REST API для магазина. В файле `main.py` уже есть модели данных и пустой список `db` с двумя товарами. 
Тебе нужно разобраться как работать с list в python и реализовать все функции вместо `pass`.

### Что нужно реализовать

| Метод | URL | Что делает | Код ответа |
|-------|-----|------------|------------|
| GET | `/` | Проверка работоспособности, возвращает `{"status": "ok"}` | 200 |
| GET | `/items` | Список всех товаров | 200 |
| GET | `/items/{id}` | Один товар по id | 200 / 404 |
| POST | `/items` | Создать товар, тело: `{"name": "...", "price": ...}` | 201 |
| DELETE | `/items/{id}` | Удалить товар по id | 204 / 404 |

### Запуск

```bash
# Создать виртуальное окружение
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt

# Запустить
uvicorn main:app --reload
```

После запуска открой в браузере:
- `http://localhost:8000/items` - список товаров

### Проверка через curl 

```cURL (сокращение от "Client URL") - это мощный инструмент командной строки и библиотека для передачи данных с использованием различных сетевых протоколов. Если говорить проще: это "браузер без графического интерфейса", который позволяет отправлять запросы и получать ответы прямо из терминала.```

```bash
# Получить все товары
curl http://localhost:8000/items

# Получить один товар
curl http://localhost:8000/items/1

# Создать новый товар
curl -X POST http://localhost:8000/items \
     -H "Content-Type: application/json" \
     -d '{"name": "Клавиатура", "price": 3500}'

# Удалить товар
curl -X DELETE http://localhost:8000/items/1

# Попробовать полчить несуществующий товар - должен вернуть 404
curl http://localhost:8000/items/999
```

## Запусти тест для готового ендпойнта

```python 
pytest tests/test_main.py::test_healthcheck -v
```

---

## Docker

### Задача

Упаковать готовый API в Docker-образ и запустить его в контейнере.

### Шаг 1 - собери образ

```bash
docker build -t shop-api .
```

### Шаг 2 - запусти контейнер

```bash
docker run -d -p 8000:8000 shop-api
```

Проверь что API работает: `http://localhost:8000/items`

### Шаг 3 - изучи базовые команды docker контейнера

```bash
# Посмотреть запущенные контейнеры
docker ps

# Логи контейнера
docker logs <container_id>

# Зайти внутрь контейнера
docker exec -it <container_id> sh

# Найди где лежит main.py 
```


```bash
docker images
```

---

## Критерии приёма работы

**Задание 1:**
- [ ] Все 5 эндпоинтов реализованы
- [ ] GET `/items/999` возвращает 404, а не 500
- [ ] POST `/items` возвращает статус 201
- [ ] DELETE `/items/{id}` возвращает статус 204

**Задание 2:**
- [ ] `docker build` завершается без ошибок
- [ ] `docker run -p 8000:8000 shop-api` запускает API
- [ ] `http://localhost:8000/items` открывается в браузере - почему?
- [ ] После остановки контейнера данные пропадают - почему?

---
